if [[ $BUILD_OPTIONS == *debug* ]]; then
  ac_add_options --enable-debug
  ac_add_options --disable-optimize
else
  ac_add_options --enable-optimize
fi

if [[ $BUILD_OPTIONS == *artifact* ]]; then
  ac_add_options --enable-artifact-builds
fi
