MOZ_CO_PROJECT=browser,xulrunner                   # Selects which projects are checked out
BUILD_APP=`echo $BUILD_CONFIG | sed -e s/-.*//`    # By default strip the end off the config to get the app

#MOZ_CO_TAG=MOZILLA_1_8_BRANCH                      # Selects a branch to checkout
#MOZ_CO_DATE="2006-02-24 21:30"                     # Checkout from a specific time
