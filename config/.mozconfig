CONFIG=$BUILD_BASE/config

if [ "$BUILD_WATCH" ]; then
  ac_add_options --enable-xterm-updates
fi

if [ -f $topsrcdir/$BUILD_APP/config/mozconfig ]; then
	. $topsrcdir/$BUILD_APP/config/mozconfig
fi

if [ -f $CONFIG/global.mozconfig ]; then
	. $CONFIG/global.mozconfig
fi

if [ -f $CONFIG/$BUILD_CONFIG.mozconfig ]; then
	. $CONFIG/$BUILD_CONFIG.mozconfig
fi
