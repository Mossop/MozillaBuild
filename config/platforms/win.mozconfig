if [ "${BUILD_TYPE}" = "full" ]; then
  export CC="clang-cl.exe"
  export CXX="clang-cl.exe"
  export LINKER="lld-link.exe"
fi

if [ "${TOOLCHAIN}" = "64-bit" ]; then
  ac_add_options --target=x86_64-pc-mingw32
  ac_add_options --host=x86_64-pc-mingw32
fi
