#! /bin/sh

. $BUILD_BASE/bin/repoutils

info=`tree_info` || exit
tree=`tree $info`
repo=`repo_url $info` || exit
if [ "$1" ]; then
  bookmark=$1
else
  bookmark=`current_bookmark $tree` || exit
fi

hg -R $BUILD_BASE/source/$tree push $repo -B $bookmark
