#! /bin/bash

log() {
  :
}

. $BUILD_BASE/bin/applyconfig

if [ ! -d $MOZ_OBJDIR ]; then
  mkdir -p $MOZ_OBJDIR
fi
	
cd $BUILD_BASE/source/$BUILD_SOURCE
$BUILD_PYTHON mach build

${BUILD_BASE}/bin/ifttt build_complete `hostname`
